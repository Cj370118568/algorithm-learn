/**
 * @param {string} a
 * @param {string} b
 * @return {string}
 */
var addBinary = function (a, b) {
  var cur = 0;
  var str = "";
  while (cur < a.length && cur < b.length) {}
  if (a.length > cur) {
  }
  if (b.length > cur) {
  }
  return str;
};

var a = "11",
  b = "1";
console.log(addBinary(a, b));
